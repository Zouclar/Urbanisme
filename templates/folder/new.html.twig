{% extends 'base.html.twig' %}

{% block title %}Nouveau Dossier{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Créer un nouveau Dossier</h1>

        <div class="card">
            <div class="card-body">
                {{ include('folder/_form.html.twig') }}
            </div>
        </div>

        <a href="{{ path('app_folder_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    </div>
{% endblock %}