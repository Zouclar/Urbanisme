{% extends 'base.html.twig' %}

{% block title %}Modifier le Dossier{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Modifier le Dossier</h1>

        <div class="card">
            <div class="card-body">
                {{ include('folder/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_folder_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        <div class="mt-3">
            {{ include('folder/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}